[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gufe"
version = "0.9.0"
description = "Minimal gufe stub for lomap2 import compatibility"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["gufe"]
